using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Eraser : ScannableObject
{
    public override void OnScan()
    {
        inator.LoadInator(scannedName);
        base.OnScan();
    }
}
